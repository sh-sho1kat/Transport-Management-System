# Optional future containers

No Docker setup is required or provided at Stage 0. Use scripts/next-db.py and IntelliJ. Add containers/Testcontainers only when the relevant test/deployment milestone is implemented, documenting the optional local workflow.
