{
  "openapi": "3.0.3",
  "info": {
    "title": "Wayline Next \u2014 implemented foundation only",
    "version": "0.1.0",
    "description": "Business endpoints will use /api/v2 as each stage is implemented. No authentication or business endpoint exists yet."
  },
  "servers": [
    {
      "url": "http://localhost:8089"
    }
  ],
  "paths": {
    "/actuator/health": {
      "get": {
        "operationId": "health",
        "responses": {
          "200": {
            "description": "Application and database healthy",
            "content": {
              "application/json": {
                "schema": {
                  "type": "object",
                  "required": [
                    "status"
                  ],
                  "properties": {
                    "status": {
                      "type": "string",
                      "enum": [
                        "UP"
                      ]
                    }
                  }
                }
              }
            }
          },
          "503": {
            "description": "Application dependency unhealthy"
          }
        }
      }
    }
  },
  "components": {
    "schemas": {
      "ApiError": {
        "type": "object",
        "required": [
          "code",
          "message",
          "timestamp"
        ],
        "properties": {
          "code": {
            "type": "string"
          },
          "message": {
            "type": "string"
          },
          "timestamp": {
            "type": "string",
            "format": "date-time"
          }
        }
      }
    }
  }
}
