# Local development only. IntelliJ Run uses this profile by default.
server.address=127.0.0.1
server.port=${PORT:8088}
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://127.0.0.1:55432/wayline}
spring.datasource.username=${DATABASE_USER:wayline}
spring.datasource.password=${DATABASE_PASSWORD:WaylineDb123!}
app.allowed-origins=${ALLOWED_ORIGINS:http://localhost:5178,http://127.0.0.1:5178}
app.frontend-url=${FRONTEND_URL:http://localhost:5178}
app.admin-email=${BOOTSTRAP_ADMIN_EMAIL:admin@example.test}
app.admin-password=${BOOTSTRAP_ADMIN_PASSWORD:AdminPass123!}
app.mail-enabled=false
server.servlet.session.cookie.secure=false
